<!--suppress ALL -->
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:homeAsUpIndicator">@drawable/back_button_icon</item>
        <item name="buttonStyle">?android:borderlessButtonStyle</item>
        <item name="android:textViewStyle">?android:textViewStyle</item>
    </style>

    <style name="CustomButtonStyle" parent="AppTheme">
        <item name="android:strokeWidth">2</item>
        <item name="android:strokeColor">@color/colorWhite</item>
        <item name="android:background">@drawable/border</item>
        <item name="android:backgroundTint">@color/colorYellow</item>
    </style>

    <style name="circleSectionButton" parent="AppTheme">
        <item name="android:background">@drawable/circle_bg</item>
        <item name="android:strokeWidth">2</item>
        <item name="android:textColor">@color/colorWhite</item>
        <item name="android:strokeColor">@color/colorWhite</item>
        <item name="android:fontFamily">@font/technique_bold_rgvoy</item>
    </style>

    <style name="gridLayoutManagerStyle" parent="AppTheme">
        <item name="android:backgroundTint">@color/colorPurple</item>
    </style>
    
    <style name="DialogSlide">
        <item name="android:windowEnterAnimation">@anim/move_bottom_to_top_500</item>
    </style>
</resources>
